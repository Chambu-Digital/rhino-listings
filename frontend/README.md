Frontend Quick Start:
1. cd frontend
2. npm install
3. npm run dev
Replace images in /public/images/ with real photos.
Set VITE_API_URL in .env for backend host if different from http://localhost:5000
